# gpux idle watcher config (per node)
IDLE_MINUTES=30
NODE_URL=http://127.0.0.1:8095
HORDE_WORKER_DIR=/mnt/hdd/data/horde-worker-reGen
# HORDE_API_KEY=  # pasted into bridgeData.yaml instead (worker auth)
